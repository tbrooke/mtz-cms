# Mailgun SMTP Configuration
# Get your credentials from: https://app.mailgun.com/app/sending/domains

# SMTP Server (usually smtp.mailgun.org or smtp.eu.mailgun.org for EU)
MAILGUN_SMTP_HOST=smtp.mailgun.org

# SMTP Port (587 for TLS, 465 for SSL)
MAILGUN_SMTP_PORT=587

# SMTP Username (Default SMTP login from Mailgun dashboard)
# Format: postmaster@your-domain.mailgun.org
MAILGUN_SMTP_USER=postmaster@your-domain.mailgun.org

# SMTP Password (Default password from Mailgun dashboard)
MAILGUN_SMTP_PASSWORD=your-smtp-password-here

# Email Configuration
# Email address to send FROM
MAILGUN_FROM_EMAIL=noreply@mtzcg.com

# Email address to receive contact form notifications
MAILGUN_ADMIN_EMAIL=office@mtzcg.com
